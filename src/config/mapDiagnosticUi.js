/** UI mínima en /map: solo BUILD SHA + último MAP_CAMERA api-call. */
export const MAP_DIAGNOSTIC_UI_CLEAN = true

/** Altura aproximada del badge BUILD SHA (px) para posicionar el panel debajo. */
export const MAP_DIAGNOSTIC_BADGE_OFFSET_PX = 30
